---
type: diary
project: studio-server
date: 2026-06-04
---
- [onboard-school] (Alek) Fix bug `save_pending_school`: l'agente passava `selection.kind:"product"` (inesistente) → `invalid_union_discriminator`, salvataggio portale KO. Causa: `save_pending_school` esponeva all'LLM lo schema canonico con `selection` discriminato, mentre `add_bundle_to_portal` usa componenti piatti. Soluzione DRY: nuovo `pendingSchoolInputSchema` (componenti piatti `{productSlug, variantSku}`) + helper `toCanonicalPendingSchool()` che normalizza a `selection:{kind:"variant"}` server-side, coerente con add_bundle. L'LLM non emette più il discriminatore. Tool description corretta ("Payload" non ".md su filesystem"). Typecheck verde, normalizzazione validata contro schema canonico.
- [onboard-school] (Alek) catalog.productDiscounts: nuovo campo schema (`{slug, kind:"percent"|"eur", value}[]` nullable) per lo sconto per-prodotto raccolto dal ProductPicker. Prompt aggiornato: l'agente riporta selectedSlugs in catalog.visibleSlugs e productDiscounts in catalog.productDiscounts. toCanonicalPendingSchool/toPayloadDoc passano il catalog wholesale → persistito su Payload. Solo registrato (no Saleor). typecheck verde.
- [onboard-school] (Alek) Fix affidabilita' sconti: l'LLM ometteva in modo intermittente catalog.productDiscounts in save_pending_school (scuola-dei-joshua OK, monaci-francescani NULL). Ora `extractPickerDiscounts()` legge gli sconti DETERMINISTICAMENTE dall'ultima submission ProductPicker (messaggio generative_submission) e li inietta nel doc prima di scrivere su Payload, bypassando l'LLM. typecheck verde.
- [portals/onboard] (Alek) Fix 2 bug. (1) reader.toDetail scartava catalog.productDiscounts → il dettaglio portale non riceveva gli sconti (dato OK in Payload/mail). Ora normalizzato e incluso nel PortalDetail.catalog. (2) prompt: regola UI ferrea "un solo render_* per messaggio; dopo render_logo_uploader fermati e aspetta" per ridurre il chaining logo+picker.
