---
type: diary
project: kyron-studio-server
date: 2026-05-26
---

- [19:30] (Alek) **BFF gateway + agente Editor Dati live**. Bootstrap documentazione locale (prima non esisteva `documentation/` qui). Creata feature 001-bff-gateway (endpoint `/api/v1/collections/*`, auth a 2 livelli X-Tenant + kyron-rev HMAC, service-to-service Payload via API Key, registry 8 collection + search field map per-collection per evitare 400 su products/brands). Creata feature 002-data-editor-agent (SSE `/agents/data-editor`, 5 tool delegati al gateway, system prompt italiano con pattern read-before-write, fallback model openai/gpt-4o via resolveModel). Aggiornato CLAUDE.md: nuove route mount, gotcha critici sezione (AI SDK maxSteps + search field map + API Key RBAC). KEY INSIGHT (bug catturato live): `streamText` AI SDK v4 default `maxSteps: 1` → l'agente fa il primo tool call e si ferma senza produrre testo. Sempre `maxSteps: 8` (o simile) per agenti multi-step. Documentato in feature 002 come regola per i prossimi agent.
