---
type: diary
project: studio-server
date: 2026-06-25
---
- [13:58] (Alek) Fix duplicazione portali KO (400 su POST /api/v1/portals/:slug/duplicate). CAUSA: la collection Payload `pending-schools` ha 5 campi `schoolAddress` obbligatori (streetAddress1, postalCode, city, countryArea, country) e nessun draft versioning, quindi i `required` sono validati anche per una Bozza. `buildClonedDoc` (writer.ts) svuotava l'indirizzo a `{country:"IT"}` → `gateway.create` riceveva un ValidationError da Payload che `errorResponse` rigirava come 400 ("Duplicazione fallita. Riprova.") → OGNI duplicazione falliva, qualunque slug. FIX: `ADDRESS_PLACEHOLDER` con "TBD" (stessa convenzione di codiceMeccanografico) sui 4 campi mancanti. INSIGHT: i campi `required` Payload vanno popolati anche per le bozze finche' la collection non ha `versions: { drafts: true }` — svuotare != opzionale. Aggiunto test di regressione `tests/features/portals-duplicate.test.ts` (verifica che tutti i campi indirizzo required siano valorizzati). Doc: feature 007 aggiornata. DA FARE: redeploy studio-server in prod (Coolify, app uuid x5bzjhuxbl4ab4j5tnkbckq0) — il fix non e' live finche' non si redeploya.
