---
type: diary
project: kyron-studio-server
date: 2026-08-25
---

- [18:30] (Alek) Nuovo agente **Statistiche (Ada)** su `/agents/stats` — `src/features/stats-agent/{hogql-guard,prompt,agent,route}.ts`. Scrive HogQL da sola su PostHog in sola lettura. Il vincolo vero e' il rate limit della Query API (~120 query/ora per key, condivisa con `/analytics` e il report delle 09:00): tool `overview` per le domande standard (legge la cache di `getOverview`, zero query) + budget in memoria di 40 query/ora per `run_hogql`. Guard `assertReadOnly`: solo SELECT/WITH, una istruzione, lista nera DDL/DML con match `\b...\b` (cosi' `updated_at` passa), no `url()/file()`, `LIMIT 200` forzato. 13 test verdi in `tests/features/stats-hogql-guard.test.ts`. Aggiunto `runHogqlWithColumns` accanto a `runHogql` senza toccarlo: senza i nomi delle colonne il client non puo' etichettare tabella e grafico, e `runHogql` lo usano 8 query di `/analytics`. Doc unica in `../studio/documentation/features/017-stats-agent.md`. **Serve un redeploy** perche' Ada esista in prod.
