---
type: diary
project: studio-server
date: 2026-06-14
---
- [10:00] (Alek) Feature 007 — report ordini giornaliero via email alle 09:30 Europe/Rome (ordini di ieri da Saleor prod, raggruppati per portale, con SKU+descrizione e totale). Companion del report analytics delle 09:00 (feature 005). Decisioni: esclude gli ordini di test interni (email in `ORDERS_REPORT_EXCLUDE_EMAILS`), manda comunque "0 ordini" nei giorni vuoti.
- [10:00] (Alek) DRY: estratti `src/core/email/mailer.ts` (sendKyronEmail/logo cid/recipientsFromEnv) e `src/core/scheduler.ts` (armDailyJob/romeNow/romeYesterday) dal report analytics, che ora li riusa. KEY: lo scheduler generico fa catch-up tutto il giorno dopo l'orario (prima l'analytics catch-uppava solo 09:00-09:59) — piu' robusto ai restart del container.
- [10:00] (Alek) Ordini letti via Saleor GraphQL `orders` con **app token MANAGE_ORDERS** (`SALEOR_APP_TOKEN`), non admin staff (vedrebbe 0 ordini per restrictedAccessToChannels — gotcha Danea). `core/saleor/client.ts` ora esporta `saleorApiUrl()`. typecheck verde, 19 test passano (i 3 file falliti sono preesistenti: package `postgres` mancante + modulo legacy clients). Smoke render 2 portali ok.
- [10:00] (Alek) DA DEPLOYARE (push main → autodeploy): impostare su Coolify studio-server prod `SALEOR_API_URL=https://api.kyronedu.it/graphql/`, `SALEOR_APP_TOKEN`, `ORDERS_REPORT_ENABLED=true`. Test immediato: `POST /api/v1/orders-report/send` (admin). Feature doc: `documentation/features/007-orders-report.md`.
- [12:10] (Alek) Destinatario report giornalieri cambiato `info@` → **`team@kyronedu.it`** (default in codice: analytics `report.ts`, ordini `orders-report/report.ts`, + `.env.example`). ATTENZIONE: le env `ANALYTICS_REPORT_TO`/`ORDERS_REPORT_TO` su Coolify, se settate, **vincono** sul default → vanno aggiornate a mano in prod a `team@kyronedu.it,gmail@alekdob.com` (altrimenti il cambio non ha effetto). Lasciata invariata la notifica "portale live" (`portals/enable/notify.ts`, `PORTAL_LIVE_NOTIFY_TO`) — non e' un report.
- [17:00] (Alek) Feature 008 v2 — `OrderSummary` esteso con dati **cliente** (customerName/phone/address da `billingAddress`, fallback user/email) e **`pspReference`** (Stripe PaymentIntent da `transactions[]`); `enrichOrder` aggiunge **`stripeUrl`** (`dashboard.stripe.com/payments/{psp}`). Serve il drawer dettaglio + ricerca del modulo Ordini (studio feature 010 v2). Verificato su Saleor prod: pspReference = `pi_...`, billingAddress completo.
- [15:30] (Alek) Feature 008 — **API ordini** `GET /api/v1/orders` per il nuovo modulo Ordini di Studio. Lista ordini Saleor su range date (default 30g), arricchiti con **agente commerciale** (`requestedBy`), **codice meccanografico** e **link portale** via join `order.channelSlug == portal.slug` (riuso `listPortals()`/reader). Auth: tenant + studioAuth, **NO requireAdmin** (tutti i commerciali la vedono). DRY: estratto `fetchOrders` privato in `core/saleor/orders.ts`, `fetchOrdersForDay` ora lo chiama con from==to; aggiunti `status`/`paymentStatus` a `OrderSummary` (riusati anche dal report 007). Esposto `codiceMeccanografico` in `PortalSummary` (era solo in Detail). typecheck verde.
- [13:00] (Alek) **Report ordini LIVE in prod** (`studio-server.kyronedu.it`, app Coolify uuid `x5bzjhuxbl4ab4j5tnkbckq0`). PR #1 mergiata su `main`. Le 3 env settate via **Coolify API** (`ssh kyron-prod` → `curl localhost:8000`, niente UI — vedi memoria `preferisce-comandi-terminale`): `ORDERS_REPORT_ENABLED=true`, `ORDERS_REPORT_TO=team@+gmail`, `SALEOR_APP_TOKEN` copiato server-side da `storefront-prod` (senza esporlo). Redeploy via API → log confermano `orders daily report armed (09:30 Europe/Rome)`. KEY: **niente autodeploy** (Coolify Kyron senza FQDN pubblico → no webhook GitHub); i deploy si triggerano sempre a mano; Source di studio-server resta "Public GitHub" (`source_id 0`). GOTCHA: un'env settata col valore placeholder la crea ma rompe il runtime → correggere con `PATCH .../envs`. Token Coolify usato in sessione: **da ruotare**.
