---
type: diary
project: studio-server
date: 2026-06-19
---
- [Onboarding] (Alek) Fix loop "prezzi finali iPad 256/512 < listino" + portale "fermi" non trovato alla pubblicazione. CAUSA: 3 gate rifiutavano `eur value >= listino` (prezzo finale = listino = "nessuno sconto"): `agent.ts:663` e `normalize.ts:285` con `>=`, e `enable.ts:176` con `throw` se `off<=0`. Quando l'utente diceva "procedi coi prezzi di listino", l'agente mandava finale==listino → rifiutato → loop infinito; e il salvataggio portale (che passa per normalize) falliva → la pubblicazione non trovava il portale ("errore nel salvataggio iniziale"). FIX: trattare finale>=listino come "nessuno sconto su quella variante → SCARTA la voce" (non errore) in tutti e 3 i punti: `update_discounts` filtra (kept/dropped+nota), `checkDiscounts` filtra `doc.catalog.productDiscounts`+nota fixes, `applyDiscounts` fa `continue` invece di throw. Le varianti senza sconto restano a listino, l'onboarding procede. Typecheck verde. Deployato studio-server.
- [Studio/portali] (Alek) Bug "il kit si modifica ma non va online" (portale paolo-vi): i tool agente `add_bundle_to_portal`/`update_bundle` salvavano i componenti con `productSlug` derivati dal nome (`ipad`/`alimentatore`/`apple-pencil-usb-c`) invece degli slug reali Saleor (`ipada16`/`ps-25wo1cb`/`muwa3zm-a`), senza validazione: il save passava, l'enable falliva. Fix: `validateComponentsAgainstSaleor()` valida productSlug + variantSku contro `fetchCatalogIndex()` PRIMA di persistere (fail-fast all'edit, fail-open se Saleor giù), + descrizioni parametri riscritte (slug/SKU REALI). Backend typecheck verde. Vedi `gotcha-portal-kit-slug-mismatch`.
- [Catalogo] (Alek) INSIGHT: `MX2D3ZM/A` su Saleor è la **Apple Pencil _Pro_** (slug `mx2d3zm-a`), NON la USB-C. La "Apple Pencil (USB-C)" è `MUWA3ZM/A` (slug `muwa3zm-a`). I kit paolo-vi "...+ Apple Pencil USB-C" vanno corretti con `muwa3zm-a`/`MUWA3ZM/A` (il codice `MX2D3ZM/A` indicato era un refuso, confermato da Alek).
- [TODO] (Alek) UI `BundleCard` (studio) ha bug gemello su add/remove componente (slug come SKU + distrugge selection) → task separato.
- [Prod/risolto] (Alek) paolo-vi sistemato a mano via SSH (l'agente chat eseguiva una sola azione per prompt e tra l'altro `update_catalog` riscrive l'INTERA lista → ha azzerato `visibleSlugs` cancellando coverONE/Zagg e ridotto i discount al solo `muwa3zm-a`). Deciso col cliente: niente kit-pencil, pencil come accessorio singolo. Fix diretto: `patchPortalCatalog("paolo-vi", {visibleSlugs:[coverone,za103117084,muwa3zm-a], hiddenSlugs:[ipada16,ps-25wo1cb,applecare-plus-ipad-a16,dbp01-a35ri], productDiscounts: coverone→25, za103117084→89, applecare-plus-ipad-a16→75, muwa3zm-a→85})` + `enablePortal("paolo-vi",["prod"])` (channel Channel:12, recalc ok). Stato finale live: 2 kit "iPad+Alimentatore" + 3 accessori (coverONE 25€, Zagg 89€, Apple Pencil USB-C 85€ da 89), dettagli 200. GOTCHA agente: `update_catalog`/`update_discounts` sostituiscono la lista intera → un "aggiungi X" mal posto cancella il resto.
