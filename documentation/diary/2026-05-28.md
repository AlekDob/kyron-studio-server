---
type: diary
project: studio-server
date: 2026-05-28
---
- [pomeriggio→sera] (Alek) **WS05 — Portali su Payload, live**. Riscritto `src/features/portals/` come wrapper sopra `core/payload/gateway.ts` (decision-016 nel brain umbrella). Nuovo `gateway.ts` (singleton lazy su `kyronTenant` + costante `pending-schools`); `reader.ts` con `findPortalDoc` via `where[slug][equals]` (esteso gateway base per il supporto `where`); `writer.ts` PATCH parziali — array `bundles` read-modify-write con strip dell'`id` Payload-managed (`writableBundle`); `logo.ts` multipart upload a `/api/media` + link `branding.logo` (Media collection invece di filesystem); `markdown-writer.ts` ora wrapper Payload (nome storico). Aggiunto script `scripts/migrate-portals-md-to-payload.ts` idempotente per i `.md` dev residui. Dockerfile pulito (rimosso `mkdir /data/portals`). Tipi puliti, deploy `studio-server-staging` (commit `496773e`) verde. Feature 004-portals-gateway documentata. Smoke endpoint live: `GET /api/v1/portals` → `200 []`. **KEY INSIGHT**: schema Payload era gia' allineato 1:1 con `pendingSchoolSchema` Zod dell'agente — il commento "schema drift" in `markdown-writer.ts` era stale. Il vero blocker era nel cms: `PendingSchools.ts` mai committato e tabelle `pending_schools` mai pushate su Postgres staging. Risolto in catena con cms team (commit `ded03f1` + `30043ec`).
