---
type: diary
project: studio-server
date: 2026-09-03
---
- [14:45] (Alek) Modulo Richieste (Ivo): nuovo `src/core/linear/client.ts` (fetch nudo verso l'API GraphQL Linear, nessun SDK) + `src/features/requests/` (service/route/tools/prompt/agent). Tre tool: `list_requests` filtra il pannello, `draft_request` propone senza scrivere, `create_request` scrive solo con `confirm: true`. Verificato in prod-Linear: creato FUT-100 di prova e cancellato. Gotcha: Linear linkifica da sola le email nella descrizione (`[x@y.it](<mailto:x@y.it>)`), l'estrazione del richiedente usa una regex. Env `LINEAR_API_KEY` — la prima chiave incollata in chat va considerata bruciata e rigenerata. Feature 022 (doc unica lato studio).
- [15:20] (Alek) Richieste, secondo giro: l'agente chiede l'urgenza come ultima domanda e la scrive su Linear come `priority` (bloccante=1 ... bassa=4, tradotte in italiano una volta sola in `core/linear/client.ts`). All'apertura parte anche una mail ad Alek con numero, oggetto, descrizione e urgenza, riusando `sendKyronEmail` (nuovo `requests/notify.ts`, env `REQUESTS_NOTIFY_TO`). La mail e' best-effort: se Resend e' giu' il ticket resta aperto lo stesso. Verificato: FUT-103 aperto con priority Urgent e mail consegnata, poi cancellato.
