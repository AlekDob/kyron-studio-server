---
type: diary
project: studio-server
date: 2026-08-27
---
- [12:50] (Alek) Ada non sapeva cosa vendiamo: `order_completed` su PostHog porta solo numero ordine, totale e valuta — nessun nome prodotto. La torta "prodotti piu' venduti" usciva con una fetta sola chiamata "-". Nuovo tool `sales_by_product` che legge le righe d'ordine Saleor (`fetchOrdersForRange`), aggrega per nome prodotto (quantita', fatturato, ordini), esclude CANCELED e le email di test. Prompt: divieto esplicito di cercare i prodotti su PostHog, date sempre gg/mm/aaaa (raggruppare sulla data vera, formattare solo l'etichetta) e regola di proattivita' — su domanda generica sceglie lei il taglio e il grafico invece di chiedere.
- [17:16] (Alek) `plan_danea_import` falliva su XML piccoli (53 righe / 35 gruppi) con "limite di 100 record". Non era il file: il piano scaricava il catalogo Saleor con `products(first: 200)` e Saleor rifiuta ogni pagina oltre 100. Nico traduceva l'errore GraphQL come se fosse il listino. `listProducts` ora pagina a 100; l'import arriva a 500 SKU esistenti.
- [Nico prova DDT] (Alek) Chiuso il flusso di prova prima dell'invio massivo: `plan_ddt_mailing` passa alla card e al modello `testTo` (email dell'operatore loggato), `send_ddt_test_mail` e la route admin mandano una sola mail senza consumare il claim anti-doppio-invio e senza dipendere da `DDT_MAIL_ENABLED`/allowlist. Il prompt chiarisce che le tre `previews` sono esempi di dati cliente e non destinatari della prova: se l'operatore chiede chi la riceve, Nico deve citare solo `testTo`. Validazione stretta a un singolo indirizzo; test DDT verdi.
